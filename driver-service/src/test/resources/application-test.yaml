spring:
  application:
    name: driver-service
  datasource:
    url: jdbc:postgresql://localhost:5433/driver_service
    password: root
    username: postgres
  liquibase:
    enabled: false
  cloud:
    discovery:
      enabled: false
server:
  port: 8083
eureka:
  client:
    enabled: false
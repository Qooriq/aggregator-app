spring:
  application:
    name: wallet-service
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
feign:
  name:
    passenger: PASSENGER-SERVICE
    driver: DRIVER-SERVICE
server:
  port: 8086